{% if breadcrumbs.get() %}
<nav aria-label="{{ 'THEME_QUARK2.ARIA.BREADCRUMB'|t|e('html_attr') }}">
    <ol class="breadcrumbs">
        {% for crumb in breadcrumbs.get() %}
            {% if loop.last %}
                <li aria-current="page">{{ crumb.title }}</li>
            {% else %}
                <li><a href="{{ crumb.url }}">{{ crumb.title }}</a></li>
            {% endif %}
        {% endfor %}
    </ol>
</nav>
{% endif %}
