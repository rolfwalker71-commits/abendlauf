{# Das Vereinslogo als Vektor. Schiebt sich beim Laden einmal von links
   ins Bild und bleibt dann stehen. Aus dem alten PNG nachgezeichnet:
   in jeder Grösse scharf, nimmt über currentColor die Leitfarbe an. #}
<span class="runlogo">{{ svg_image('theme://img/logo.svg')|raw }}</span>
